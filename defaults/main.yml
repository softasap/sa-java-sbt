---
# defaults file for sa-java-sbt

role_dir: "{{role_path}}"

sbt_version: "1.1.1"

sbt_lib_root_path: "/usr/share/sbt"
sbt_lib_path: "{{sbt_lib_root_path}}/{{sbt_version}}"

bin_dir: "/usr/bin"
sbt_bin_link: "{{bin_dir}}/sbt"
